'use strict';

describe('hereComesTheSun.sunrise module', function() {

  beforeEach(module('hereComesTheSun.sunrise'));

  describe('sunrise controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var sunriseCtrl = $controller('SunriseCtrl');
      expect(sunriseCtrl).toBeDefined();
    }));

  });
});